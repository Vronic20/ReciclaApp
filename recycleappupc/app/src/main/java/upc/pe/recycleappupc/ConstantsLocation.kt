package upc.pe.recycleappupc

import android.Manifest

object ConstantsLocation {
    const val REQUEST_LOCATION = 50
    val REQUIRED_PERMISSION_LOCATION = arrayOf(Manifest.permission.ACCESS_COARSE_LOCATION)
}